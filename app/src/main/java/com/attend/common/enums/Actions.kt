package com.attend.common.enums

enum class Actions {
    SELECT,
    VIEW,
    REFRESH,
    ADD,
    UPDATE,
    DELETE,

    VIEW_ORDER,
    VIEW_PROVIDER,

    ACCEPT,
    REJECT,
    IN_PROGRESS,
    COMPLETE,
    CANCEL,
    REVIEW,
}